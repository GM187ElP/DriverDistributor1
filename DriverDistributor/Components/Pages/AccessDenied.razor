@inject NavigationManager Navigation

<div class="access-denied-wrapper">
    <div class="access-denied-card">
        <h1>شما اجازه دسترسی به این صفحه را ندارید</h1>
      
      <MudButton Color="Color.Info" Variant="Variant.Filled" OnClick="@(() => Navigation.NavigateTo("/"))">
            بازگشت
        </MudButton>
    </div>
</div>

<style>
    .access-denied-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        width: 100%;
        background-color: #f5f5f5;
        direction: rtl;
    }

    .access-denied-card {
        background: #fff;
        padding: 40px 30px;
        border-radius: 14px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        text-align: center;
        max-width: 400px;
        width: 100%;
    }

        .access-denied-card h1 {
            font-size: 1.8rem;
            color: #d32f2f;
            margin-bottom: 20px;
        }
</style>


<style>
    /* Full-page center alignment */
    .access-denied-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        width: 100%;
        background-color: #f5f5f5;
        direction: rtl;
    }

    .access-denied-card {
        background: #fff;
        padding: 40px 30px;
        border-radius: 14px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        text-align: center;
        max-width: 400px;
        width: 100%;
    }

        .access-denied-card h1 {
            font-size: 1.8rem;
            color: #d32f2f;
            margin-bottom: 20px;
        }

        .access-denied-card p {
            font-size: 1rem;
            margin-bottom: 30px;
            color: #555;
        }

</style>
